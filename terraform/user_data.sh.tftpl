#!/bin/bash
set -euxo pipefail

dnf install -y docker
systemctl enable --now docker

docker pull ${image_ref}
docker run -d \
  --name servicelink \
  --restart unless-stopped \
  -p ${app_port}:${app_port} \
  ${image_ref}
